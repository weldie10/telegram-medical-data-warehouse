# Core dependencies for Telegram scraping and data pipeline
telethon>=1.34.0
python-dotenv>=1.0.0

# Data processing
pandas>=2.0.0

# Logging
colorlog>=6.8.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# API and web framework (for later tasks)
fastapi>=0.104.0
uvicorn>=0.24.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pydantic>=2.5.0

# Data transformation (for later tasks)
dbt-postgres>=1.7.0

# Object detection (for later tasks)
ultralytics>=8.0.0

# Orchestration (for later tasks)
dagster>=1.5.0
dagster-webserver>=1.5.0
